/**
 * FrameSource is the decode-side contract every playable source implements
 * (procedural animation, video decoders, ...). Interface-only module, so it
 * has no consts.ts and no implementation of its own.
 */
export * from './types.ts';
